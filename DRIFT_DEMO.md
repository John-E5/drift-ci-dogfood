# Drift-CI PR-comment demo

Throwaway file to trigger the `drift` workflow on a pull request so the live
drift-ci PR comment renders against the fixed `Drift-CI/drift-ci@v1` action.

Safe to close this PR without merging.
